version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npx ng build skycast-rx-dashboard --configuration production
  artifacts:
    baseDirectory: dist/skycast-rx-dashboard/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
